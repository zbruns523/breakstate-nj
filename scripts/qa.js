const fs=require('fs'),vm=require('vm'),path=require('path'),assert=require('assert');
const root=path.resolve(__dirname,'..'),app=fs.readFileSync(path.join(root,'src/app.js'),'utf8'),server=fs.readFileSync(path.join(root,'server.js'),'utf8');new vm.Script(app);new vm.Script(server);
assert(server.includes('/api/corridor'),'continuous viewport endpoint required');assert(server.includes("model:{status:'NOT APPLIED TO DEPTH'"),'unvalidated morphology must not alter depth');assert(app.includes('drawSurface'),'terrain surface renderer required');assert(app.includes('surfaceCell'),'measured-cell interpolation required');assert(!app.includes('circleMarker'),'point/circle bathymetry renderer forbidden');assert(!app.includes('Math.random'),'synthetic client depths forbidden');assert(!server.includes('Math.random'),'synthetic server depths forbidden');assert(app.includes("map.on('moveend zoomend',scheduleLoad)"),'viewport reload required');assert(server.includes('DEM_global_mosaic'),'NOAA/NCEI baseline required');assert(server.includes('ageMinutes'),'freshness guard required');
let seed=523,passed=0;function rnd(){seed=(1664525*seed+1013904223)>>>0;return seed/4294967296}function interp(z,u,v){return z[0]*(1-u)*(1-v)+z[1]*u*(1-v)+z[2]*u*v+z[3]*(1-u)*v}
for(let i=0;i<40000;i++){const z=[-rnd()*12,-rnd()*12,-rnd()*12,-rnd()*12],u=rnd(),v=rnd(),e=interp(z,u,v);assert(Number.isFinite(e));assert(e<=Math.max(...z)+1e-9&&e>=Math.min(...z)-1e-9);const wave=rnd()*360,shore=rnd()*70,h=rnd()*8,T=2+rnd()*20,a=((wave-shore+540)%360)-180,r=Math.max(-80,Math.min(80,a))*Math.PI/180,index=Math.sin(2*r)*h*h*T;assert(Number.isFinite(index));passed++}
console.log(`PASS V57 SURFACE: ${passed.toLocaleString()} deterministic interpolation/forcing states; syntax; no circles; no synthetic depth; fail-closed modeled depth`);
